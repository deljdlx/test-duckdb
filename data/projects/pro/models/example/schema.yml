
version: 2

models:
  - name: addresses
    columns:
      - name: id
        tests:
          - not_null
          - unique
      - name: address
      - name: postalCode
      - name: city

  - name: persons
    columns:
      - name: address_id
        tests:
          - not_null
